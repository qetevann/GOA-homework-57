
// 1)დაწერე ფუნქცია, რომელიც იღებს არგუმენტად რიცხვების სიას 
// და ახალ სიაში ამატებს მხოლოდ ლუწ რიცხვებს. 
// შემდეგ იპოვეთ ამ სიაში მყოფი რიცხვების ჯამი


function even(num){
  let list = []


    if (num % 2 == 0) {
      even.push(num)
    }
  }

  let sum = 0

    sum += n

even([3, 8, 12, 5, 7, 10, 4])




// 2) 2)შექმენი ფუნქცია, რომელიც იღებს არგუმენტად სტუდენტების სახელების სიას და სტუდენტის სახელს და ამ სიას ბოლოში ამატებს სტუდენტის სახელს. 

function names(nam, newstudent){
  let namlist = ['luka', 'nini', 'nia']
  nam.push(newstudent)
  console.log(namlist)

  
}





// 3)შექმენი ფუქნცია რომელიც იღებს მოსწავლეების სიას,შენი დავალებაა რომ გაიგო 
// --> თუ რომელიმე მოსწავლის სახელი იწყება ასო 'გ' ზე
//  მაშინ ასეთი მოსწავლის სახელი შეცვალეთ false მნიშვნელობით

function replace(students){
  for (let i = 0; i < students.length; i++){
    if (students[i].startsWith('გ')){
      students[i] = false
    }
  }
  return students
}




// 4)შექმენი ფუნქცია რომელიც იღებს სტრინგს(სტრინგის სიგრძე მეტი უნდა იყოს 10 ზე) 
// და რიცხვს,შენი დავალებაა რომ ამ სტრინგიდან გამოიტანო 0 ინდექსიდან 
// ფუნქციის პარამეტრში გადაცემულ რიცხვამდე(ინდექსამდე) მყოფი ასოები, 
// გამოიყენე ნასწავლი ფუნქცია


function cut(text, num) {
  if (text.length <= 10) {
    console.log('string length should be mkre than 10')
  }

  return text.slice(0, num)
}



// 5)შექმენი ფუნქცია რომელიც იღებს სტრინგების სიას,
// შენი დავალებაა რომ შეამოწმო,თუ რომელიმე სტრინგის სიგრძე არის 
// ლუწი რიცხვისგან შემდგარი მაშინ ასეთი სახელები ჩაამატეთ ახალ სიაში,
// ხოლო თუ რომელიმე სტრინგის სიგრძე არის კენტი რიცხვისგან შემდგარი ასეთი სტრინგები შეცვალე false მნიშვნელობით


function evenodd(strings){
  let even = [] 

  for (let i = 0; i < strings.length; i++){
    let str = strings[i]
    if (str.length % 2 === 0){
      even.push(str) 
    } else {
      strings[i] = false 
    }
  }
}



// 6)დაწერე ფუნქცია, რომელიც იღებს სტუდენტების სახელების სიას და აბრუნებს(ითვლის) რამდენი სახელი იწყება ასო „გ“-ზე.


function g(students){
  let count = 0

  for (let i = 0; i < students.length; i++){
    if (students[i].g('გ')){
      count++
    }
  }
  console.log(count)

}


// 8)დაწერე ფუნქცია, 
// რომელიც იღებს სახელების სიას და აბრუნებს ახალ სიას მხოლოდ იმ სახელებისგან შემდგარს,
//  რომლებსაც სიგრძე 5-ზე მეტია და არის ლუწი.შემდეგ შეამოწმეთამ ახალ სიაში მყოფი სახელებიდან 
// თუ რომელიმე იწყება ასო "ა" ზე ასეთი ელემენტები ამოშალეთ სიიდან(ამაში გამოიყენეთ splice ფუნქცია,თუ მიხვდებით როგორ)


function a(names) {
  let list = names.filter(name => name.length > 5 && name.length % 2 === 0)

  for (let i = 0; i < list.length; i++) {
    if (list[i].startsWith('ა')) {
      list.splice(i, 1)
      i-- 
    }
  }

}


//9)შექმენით ფუნქცია რომელიც არგუმენტად იღებს ორ სიას(რიცხვების),
// თქვენი დავალებაა რომ იპოვოთ ორივე სიაში მყოფი რიცხვების ჯამი და შემდეგ შეადაროთ,
// თუ ამ ორი რიცხვის ჯამი მეტია 50 ზე და არის ლუწი მაშინ გამოიტანე შემდეგი ტექსტი 
// --> "ორი რიცხვის ჯამი არის მაღალი და ეს რიცხვია ..." 
// სხვა შემთხევაში გამოიტანეთ ---> 'ორი რიცხვის ჯამი არის ძალიან დაბალი და ეს რიცხვია ..."


function c(arr1, arr2){
  let sum1 = arr1.reduce((sum, num) => sum + num, 0)
  let sum2 = arr2.reduce((sum, num) => sum + num, 0)

  let total = sum1 + sum2

  if (total > 50 && total % 2 === 0){
    console.log(`the sum is high and it is ${total}`)
  } else {
    console.log(`the sum is low and it is ${total}`)
  }
}